services:
  - type: web
    name: api-clima-rd
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    envVars:
      - key: WEATHER_API_KEY
        value: "802be05831a4466e888144515252910"
